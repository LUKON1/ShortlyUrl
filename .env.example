# ShortlyURL - Self-Hosted Edition
# Copy this file to .env and fill in the values

# === Frontend (Vite) ==========================================================
# API URL used by the frontend at build time.
# In Docker (production): use relative path /api - Nginx proxies /api -> backend
# In dev (npm run dev): use absolute http://localhost:3000/api
VITE_API_BASE_URL=/api

# Public base URL of your service (used to build short links)
# Change to your server IP or internal domain, e.g. http://192.168.1.10
VITE_BASE_URL=http://localhost

# === Backend (Node.js / Express) ==============================================
# MongoDB connection string - points to the local container in docker-compose.yml
DB_URI=mongodb://mongo:27017/shortlyurl

# Public hostname of the backend (used for redirects to /pau and /exp pages)
# Should match VITE_BASE_URL
HOST_NAME=http://localhost

# Secret key for signing JWT access tokens (min 32 chars, use a random string)
JWT_SECRET=change_this_to_a_random_secret_string

# Secret key for signing JWT refresh tokens (different from JWT_SECRET)
JWT_REFRESH_SECRET=change_this_to_another_random_secret

# Express server port (must match docker-compose backend ports mapping)
PORT=3000

# Node environment - affects JWT cookie secure flag
# Use "production" in Docker, "development" for npm run dev
NODE_ENV=production