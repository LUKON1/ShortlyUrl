# ShortlyURL — Self-Hosted Edition
# Copy this file to .env and fill in the values

# ─── Frontend (Vite) ──────────────────────────────────────────────────────────
# Internal API URL used by the frontend at build time
VITE_API_BASE_URL=http://localhost:3000/api

# Public base URL of your service (used to build short links)
# Change to your server IP or internal domain, e.g. http://192.168.1.10
VITE_BASE_URL=http://localhost

# ─── Backend (Node.js / Express) ─────────────────────────────────────────────
# MongoDB connection string — points to the local container defined in docker-compose.yml
# Format: mongodb://<host>:<port>/<database>
DB_URI=mongodb://mongo:27017/shortlyurl

# Public hostname of the backend (used for redirects, e.g. paused/expired pages)
# Should match VITE_BASE_URL if frontend and backend share an origin
HOST_NAME=http://localhost

# Secret key for signing JWT tokens — use a strong random string (min 32 chars)
JWT_SECRET=change_this_to_a_random_secret_string

# Refresh token secret — different from JWT_SECRET
REFRESH_TOKEN_SECRET=change_this_to_another_random_secret

# Express server port (should match docker-compose ports mapping)
PORT=3000

# ─── Optional ─────────────────────────────────────────────────────────────────
# SMTP for email notifications (leave empty to disable)
# SMTP_HOST=
# SMTP_PORT=
# SMTP_USER=
# SMTP_PASS=